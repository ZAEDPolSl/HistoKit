# Artifact detection

This section describes artifact detection methods available in HistoKit.

## Available methods

| Method | Detects | Notes |
|---|---|---|
| GrandQC | WSI quality artifacts | TODO |

## Adding a new method

Create a new Markdown file in:

```text
docs/examples/artifact_detection/
```

and add it to the `Artifact detection` section in `mkdocs.yml`.
